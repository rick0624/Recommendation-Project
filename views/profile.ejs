<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include ("partials/header") %>
    <title>Login</title>
  </head>
  <body>
    <%- include ("partials/nav") %>
    <h1>Welcome to <%= user.name %>'s page.</h1>
    <img src="<%= user.thumbnail %>" alt="">
    <p><%= user.googleID %></p>
    <p><%= user.date %></p>
      
    <% if (good_details.length > 0) { %> <% for (let i = 0; i < good_details.length; i++) { %>
      <div class="card" style="width: 18rem">
        <div class="card-body">
          <h5 class="card-title"><%= good_details[i].name %></h5>
          <h5 class="card-title"><%= good_details[i].description %></h5>
          <h5 class="card-title"><%= good_details[i].image %></h5>
        </div>
      </div>
    <% } %> <% } %>
  </body>
</html>
